click(random(832,1050),random(756,812);  //狗粮关卡按钮位置，默认为LS-2;
//用了随机，也许能避免脚本检测？下面还没弄
//Tap(515,857)  //LS-1
//Tap(1239,620) //LS-3，其他关卡闲鱼，没打过
sleep(1500);  //等待加载出开始按钮
for(var i = 0;i < 20; i++){
  click(2150, 1000); //点击“开始行动”
  sleep(3000);  //等待加载出人员配置页面，需要联网，网络不好应适当调高
  click(1867, 740);  //点击“开始行动”
  sleep(100000);  //通关时间，自己掐秒表记一下，单位为毫秒（1s = 1000ms）
  click(1770, 157);  //预留给升级页面，点击任意位置
  sleep(300)
  click(1770, 157);  //点击任意位置退出任务
  sleep(1500);
}
